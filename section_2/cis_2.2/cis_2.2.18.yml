---

{{ if .Vars.amzn2023cis_rule_2_2_18 }}
  {{ if .Vars.amzn2023cis_level_1 }}
    {{ if eq .Vars.amzn2023cis_rsync_server "remove" }}
package:
  rsync:
    title: 2.2.18 | Ensure rsync-daemon is not installed or the rsyncd service is masked | Package
    installed: false
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 2.2.18
      NISTSP800-53-R5:
      - CM-6
      - CM-7
    {{ end }}
    {{ if eq .Vars.amzn2023cis_rsync_server "mask" }}
service:
  rsync:
    title: 2.2.18 | Ensure rsync-daemon is not installed or the rsyncd service is masked | Service
    enabled: false
    running: false
    skip: false
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 2.2.18
      NISTSP800-53-R5:
      - CM-6
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
