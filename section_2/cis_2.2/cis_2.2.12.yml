---

{{ if not .Vars.amzn2023cis_snmp_server }}
  {{ if .Vars.amzn2023cis_level_1 }}
    {{ if .Vars.amzn2023cis_rule_2_2_12 }}
package:
  net-snmp:
    title: 2.2.12 | Ensure net-snmp is not installed or the snmpd service is not enabled
    installed: false
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 2.2.12
      NISTSP800-53-R5: CM-7
    {{ end }}
  {{ end }}
{{ end }}
