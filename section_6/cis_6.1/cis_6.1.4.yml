---

{{ if .Vars.amzn2023cis_level_1 }}
  {{ if .Vars.amzn2023cis_rule_6_1_4 }}
command:
  group_perms:
    title: 6.1.4 | Ensure permissions on /etc/passwd- are configured
    exec:  stat -L -c "%a %U %G" /etc/group
    exit-status: 0
    stdout:
    -  '/6[0,4][0,4] root root/'
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 6.1.4
      NISTSP800-53-R5:
      - AC-3
      - MP-2
  {{ end }}
{{ end }}
